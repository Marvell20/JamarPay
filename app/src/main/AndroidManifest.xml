<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JamarPay"
        tools:targetApi="33" >
        <!--<activity
            android:name=".ActivityBecomeSdk"
            android:exported="false" />-->
        <activity
            android:name=".ActivityBecomeSdk"
            android:exported="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="common.CustomerVerification" />
        <activity android:name="common.LoadingView" />
        <activity android:name="common.ConfirmedIdentity" />
        <activity android:name="common.UnconfirmedIdentity" />
        <activity android:name="common.DeviceProvisioning" />
        <activity android:name="common.DeviceNotProvisioning" />
        <activity android:name="common.DeviceAlreadyProvisioned" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>